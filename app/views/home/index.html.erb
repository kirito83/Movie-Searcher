<h2 align="center">Quel film recherchez vous ?</h2>
<br>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
        <%= form_tag(home_search_url, method: :post) do %>
            <%= label_tag :search, "Entrez le nom du film recherché" %>
                <%= text_field_tag :search, params[:search],class: 'form-control'  %>
                <br>
                    <%= submit_tag "Rechercher", class: 'btn btn-primary'%>
                        <% end %>
    </div>
</div>